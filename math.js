// add the numbers of this array using .map

let array = [3,5,2]
let total = array.map(a => a + 2);
console.log(total)

console.log('-----------------------------------')

// multiply the numbers of this array
